var fs = require('fs-extra');
var path = require('path');

(function () {    
    const dist = path.resolve('.', 'dist');
    fs.removeSync(dist);
    fs.ensureDirSync(dist);
    console.log('Cleaned!');
})();